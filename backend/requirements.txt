fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
groq>=0.4.0
numpy>=1.26.0
# Note: For the Hindsight memory client, if it is distributed via a custom package 
# or local directory, install it via: pip install <path-to-hindsight-library>
# hindsight-client
